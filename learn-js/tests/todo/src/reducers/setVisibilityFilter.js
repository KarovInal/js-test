import { VisibilityFilters } from '../constants/index.js';
import { SET_VISIBILITY_FILTER }
export default function setVisibilityFilter(state = VisibilityFilters.SHOW_ALL, action) {
  switch(action.type) {
    case VisibilityFilters.SHOW_ALL:
      return 
  }
}